Litlas

Done:
- Render interactive leaflet/geojson map
- Create sidebar list to display saved books
- Re-render map colors according change of book status (to be read, read)
- Get data from Google Books API (didn't like the data I got so maybe stick to mock json file for demo?)

To do:
- Display numbers of countries read (maybe nav or sidebar list?)
- Connect to database and create localstorage/guestId to persist data?
- Touch up CSS, not happy with color palette and syntax is not DRY